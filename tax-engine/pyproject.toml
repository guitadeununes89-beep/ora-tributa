[project]
name = "tax-engine"
version = "0.1.0"
description = "Independent deterministic tax domain engine"
requires-python = ">=3.12,<3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

