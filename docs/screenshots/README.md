# Evidências visuais — Etapa 8.1

As screenshots reais ainda não foram produzidas. O runtime seguro do navegador encerrou antes da
abertura da aplicação por falha do sandbox Windows ao aplicar ACLs
(`helper_unknown_error: apply deny-read ACLs`). A falha foi reproduzida após o redesign.

Nenhum mockup ou imagem fabricada substitui essa evidência. A aplicação foi validada por HTTP, API,
testes de componentes, lint, TypeScript e build. Em host compatível, capturar em resolução desktop:

- `01-login.png`;
- `02-dashboard.png`;
- `03-produtos.png`;
- `04-produto-detalhe.png`;
- `05-catalogo-cst-cclasstrib.png`;
- `06-consulta-ibscbs.png`;
- `07-resultado-conclusivo.png`;
- `08-resultado-necessita-validacao.png`;
- `09-cobertura-nacional.png`;
- `10-empresas.png`;
- `11-curadoria.png`.

As capturas devem mostrar a interface realmente executada, com a paleta marinho/laranja adotada na
Etapa 8.1, e não podem conter credenciais, documentos reais ou dados pessoais.
